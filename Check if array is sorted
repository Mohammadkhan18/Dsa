class Solution {
  public:
    bool arraySortedOrNot(int arr[], int n) {
        int min = arr[0];
        for(int i =0; i<n;i++){
            if(min > arr[i]) return 0;
            min= arr[i];
        }
        return 1;
    }
};
