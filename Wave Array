class Solution{
    public:
    void convertToWave(int n, vector<int>& arr){
       for(int i=0;i<arr.size()-1;i++)
        {
            if(arr[i+1]>arr[i])
            {
                swap(arr[i],arr[i+1]);
                i++;
    }
};
